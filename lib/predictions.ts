import { v4 as uuidv4 } from 'uuid';
import type { Prediction } from './types';

// These functions are now handled by API routes in app/api/predictions/route.ts
// You can keep type definitions or utility functions here if needed elsewhere.
